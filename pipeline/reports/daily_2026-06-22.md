# Daily Report — 2026-06-22 (dry-run validation)

This run was a **manual dry-run** to validate the pipeline end-to-end before scheduling. It
processed one representative job rather than a full daily batch.

## Sources
- vanshb03/Summer2027-Internships — fetched OK (HTTP 200, 74 table rows)
- vanshb03 OFFSEASON_README — fetched OK (HTTP 200)
- cvrve/New-Grad — fetched OK (HTTP 200)
- intern-list.com (aiml/da/swe) — not exercised in dry-run (JS-rendered, best-effort)

## Observations
- Source data is sparse/aged: newest `Date Posted` ≈ "May 22" (~1 month old). Confirms the
  decision to use the `seen_jobs.json` dedupe ledger as the freshness signal rather than a hard
  recent-date cutoff (a tight window would yield zero jobs).
- Many application links are dead/expired (the validated job's ICE link returned 404). The
  inferred-JD fallback path was exercised and works.

## Tailored (1)
| Company | Role | Field | Link | JD |
|---|---|---|---|---|
| Intercontinental Exchange (ICE) | Artificial Intelligence, Data & Machine Learning Intern | AI-MLE | https://careers.ice.com/jobs/12830 | inferred (link 404) |

Output: `applications/AI-MLE/YuanxiCheng_IntercontinentalExchange_AIDataMLIntern/`
- `resume.tex` compiles to **exactly 1 page** ✓ (verified with `latexmk`)
- metadata block present ✓ · `job.md` snapshot present ✓ · ledger updated ✓

## Validation outcome
End-to-end chain works: fetch → parse (incl. `<a href>` extraction, `↳` continuation, 🔒 skip)
→ filter → KB routing via `00_INDEX.md` → tailor (edit-don't-replace, length-neutral) →
metadata → 1-page compile. Hardened the one-page rule to be **safe-by-construction** (edited
bullets must be ≤ the replaced bullet's length) since the cloud agent can't compile.

## KB gaps noticed
- `knowledgebase/capital-one.md` is an in-progress record (role started Jun 2026); add concrete
  metrics/projects as the internship develops for sharper tailoring.
